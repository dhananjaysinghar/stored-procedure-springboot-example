spring:
  application:
    name: StoredProcedureApplication
  jpa:
    show-sql: true
    open-in-view: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: root